language: java

sudo: required
services:
  - docker

addons:
  apt:
    packages:
      - cmake
      - cmake-data
      - libglib2.0-0
      -  glib2.0

before_install:
  - docker build -f Dockerfile.amd64 -t agileiot/agile-core-x86_64 .
install:
      - CC=clang CXX=clang++ CMAKE_C_COMPILER=clang CMAKE_CXX_COMPILER=clang++ scripts/install-deps.sh

